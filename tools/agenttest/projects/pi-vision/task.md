当前目录有三个文件,按顺序完成:

1. 读取 `data.csv`,计算 value 列的总和。
2. 用 read 工具查看 `image.png`(一个四象限纯色图),确定左上角象限的颜色(回答英文颜色名,小写)。
3. 用 read 工具查看 `digits.png`(黑底白字数字图),读出图中的数字。
4. 把三个结果按 `<总和>,<颜色>,<数字>` 的格式写入 `result.txt`,例如 `123,blue,7`。

全部完成后,最后一行只回复 DONE。
