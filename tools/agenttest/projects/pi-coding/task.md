在当前目录完成以下任务:

1. 写一个 `fizzbuzz.py`,打印 1 到 20 的 FizzBuzz(3 的倍数打 Fizz,5 的倍数打 Buzz,同时是打 FizzBuzz,否则打数字),每行一个。
2. 用 `python3 fizzbuzz.py` 运行它,确认输出正确。
3. 把输出的总行数(一个数字)写入 `result.txt`。

全部完成后,最后一行只回复 DONE。
